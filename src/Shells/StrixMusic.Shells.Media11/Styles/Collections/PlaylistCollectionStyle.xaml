<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:collections="using:StrixMusic.Shells.Media11.Controls.Collections"
    xmlns:items="using:StrixMusic.Shells.Media11.Controls.Items"
    xmlns:viewModels="using:StrixMusic.Sdk.ViewModels"
    xmlns:appModels="using:StrixMusic.Sdk.AppModels"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:converters="using:OwlCore.WinUI.Converters"
    xmlns:Media11colvms="using:StrixMusic.Shells.Media11.ViewModels.Collections"
    x:Class="StrixMusic.Shells.Media11.Styles.Collections.PlaylistCollectionStyle">

    <!--<converters:ItemClickEventArgsToClickedItemConverter x:Key="ItemClickEventArgsToClickedItemConverter" />-->

    <DataTemplate x:DataType="collections:Media11PlaylistCollection" x:Key="Media11PlaylistCollectionTemplate">
        <GridView x:Name="PART_Selector" ItemsSource="{x:Bind ViewModel.PlaylistCollection.Playlists, Mode=OneWay}" Padding="16,16,16,112"
                  HorizontalAlignment="Stretch" SelectionMode="None" IsItemClickEnabled="True">
            <!--<interactivity:Interaction.Behaviors>
                                <core:EventTriggerBehavior EventName="ItemClicked">
                                 <core:InvokeCommandAction Command="{x:Bind NavigateToPlaylistCommand, Mode=OneWay}" InputConverter="{StaticResource ItemClickEventArgsToClickedItemConverter}" />
                                </core:EventTriggerBehavior>
                                </interactivity:Interaction.Behaviors>-->
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Loaded">
                    <core:InvokeCommandAction Command="{x:Bind ViewModel.PlaylistCollection.InitPlaylistCollectionAsyncCommand, Mode=OneWay}" />
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
            <GridView.Header>
                <Grid>
                    <ProgressRing IsActive="{x:Bind ViewModel.PlaylistCollection.PopulateMorePlaylistsCommand.IsRunning, Mode=OneWay}"/>
                    <ContentPresenter x:Name="PART_EmptyContentPresenter"/>
                </Grid>
            </GridView.Header>
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="appModels:IPlaylist">
                    <items:Media11PlaylistItem Playlist="{x:Bind}"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </DataTemplate>

    <Style x:Key="Media11PlaylistCollectionStyle" TargetType="collections:Media11PlaylistCollection">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="collections:Media11PlaylistCollection">
                        <ContentControl HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                                        ContentTemplate="{StaticResource Media11PlaylistCollectionTemplate}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style BasedOn="{StaticResource Media11PlaylistCollectionStyle}" TargetType="collections:Media11PlaylistCollection"/>

</ResourceDictionary>
